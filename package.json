{
  "name": "socialbridge-mern",
  "version": "1.0.0",
  "description": "SocialBridge — Full MERN Stack Version",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev-server": "cd server && npm run dev",
    "dev-client": "cd client && npm run dev",
    "worker": "cd server && npm run worker",
    "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\" \"npm run worker\"",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}